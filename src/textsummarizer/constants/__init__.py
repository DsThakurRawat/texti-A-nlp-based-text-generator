from pathlib import Path

CONFIG_FILE = Path("config/config.yaml")
CONFIG_FILE_PATH = CONFIG_FILE  # alias for compatibility

PARAMS_FILE = Path("params.yaml")
PARAMS_FILE_PATH = PARAMS_FILE  # alias for compatibility